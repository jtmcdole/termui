name: pty2
description: Pty2 for Dart and Flutter. Provides the ability to create processes
  with pseudo terminal file descriptors.
version: 0.5.0
repository: https://github.com/jtmcdole/termui/tree/main/packages/pty2

resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  ffi: ^2.2.0
  lints: ^6.1.0
  win32: ^6.0.0

dev_dependencies:
  coverage: ^1.15.1
  test: ^1.17.0
