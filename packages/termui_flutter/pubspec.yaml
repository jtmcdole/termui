name: termui_flutter
description: Flutter integration package for termui, containing the reusable Terminal widget and GPU-accelerated canvas renderer.
version: 0.1.0
repository: https://github.com/jtmcdole/termui/packages/termui_flutter

topics:
  - cli
  - tui
  - terminal
  - console

resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  termui: ^0.1.0
  characters: ^1.4.0
